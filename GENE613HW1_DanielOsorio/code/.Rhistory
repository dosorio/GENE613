}
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI))),b1_ADGonDDMI=with(data,lm(ADG,DDMI)))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI))),b1_ADGonDDMI=with(data,lm(ADG,DDMI)))
}
hw1pt2(hw1Data)
with(data,lm(ADG,DDMI)
)
with(hw1Data,lm(ADG,DDMI))
with(hw1Data,lm(ADG~
DDMI))
with(hw1Data,lm(0+ADG~DDMI))
with(hw1Data,lm(0+ADG~DDMI))
with(hw1Data,lm(0,ADG~DDMI))
with(hw1Data,lm(0~
ADG+DDMI))
with(hw1Data,lm(ADG~DDMI))
with(hw1Data,lm(ADG~DDMI))[[1]]
with(hw1Data,lm(ADG~DDMI))[[1]][2]
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI))),b1_ADGonDDMI=with(hw1Data,lm(ADG~DDMI))[[1]][2])
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI))),b1_ADGonDDMI=with(hw1Data,lm(ADG~DDMI))[[1]][2])
}
hw1pt2(hw1Data)
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI))),b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][2]))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI))),b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][2]))
}
hw1pt2(hw1Data)
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][2]))
}
hw1pt2(hw1Data)
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][2])))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][2])))
}
hw1pt2(hw1Data)
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]])))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]])))
}
hw1pt2(hw1Data)
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]])),b1_DDMIonADG=(with(hw1Data,lm(DDMIÃDG))[[1]][[2]])))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]])),b1_DDMIonADG=(with(hw1Data,lm(DDMIÃDG))[[1]][[2]])))
}
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]]))))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]]))))
}
hw1pt2(hw1Data)
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
}
hw1pt2(hw1Data)
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
}
hw1pt2(hw1Data)
return(rbin(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
return(rbind(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(rbind(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
}
hw1pt2(hw1Data)
return(as.data.frame(rbind(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]]))))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(as.data.frame(rbind(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]]))))
}
hw1pt2(hw1Data)
return(as.data.frame.array(rbind(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]]))))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(as.data.frame.array(rbind(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]]))))
}
hw1pt2(hw1Data)
c
c
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
}
hw1pt2(hw1Data)
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~0+DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~0+DDMI))[[1]][[2]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
}
hw1pt2(hw1Data)
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~0+DDMI))[[1]][[1]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~0+DDMI))[[1]][[1]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~ADG))[[1]][[2]])))
}
hw1pt2(hw1Data)
with(hw1Data,lm(ADG~DDMI))[[1]][2]with(hw1Data,lm(ADG~0+DDMI))
with(hw1Data,lm(ADG~0+DDMI))
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~0+DDMI))[[1]][[1]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~0+ADG))[[1]][[2]])))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~0+DDMI))[[1]][[1]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~0+ADG))[[1]][[2]])))
}
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~0+DDMI))[[1]][[1]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~0+ADG))[[1]][[1]])))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data){
return(c(covariance=with(data,cov(ADG,DDMI)),
correlation=with(data,cor(ADG,DDMI)),
b1_ADGonDDMI=(with(hw1Data,lm(ADG~0+DDMI))[[1]][[1]]),
b1_DDMIonADG=(with(hw1Data,lm(DDMI~0+ADG))[[1]][[1]])))
}
hw1pt2(hw1Data)
# Execute the following code (you may need to modify the trait names to the right
# of the <- depending upon how you input your data) in R
no3.adg.ddmi <-  whith(hw1Data, lm(ADG~DDMI))
# Execute the following code (you may need to modify the trait names to the right
# of the <- depending upon how you input your data) in R
no3.adg.ddmi <-  with(hw1Data, lm(ADG~DDMI))
summary(no3.adg.ddmi)
no3.ddmi.adg <-  with(hw1Data, lm(DDMI~ADG))
summary(no3.ddmi.adg)
hw1pt2(hw1Data)
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (with(hw1Data, lm(ADG ~ DDMI))[[1]][[1]]),
b1_DDMIonADG = (with(hw1Data, lm(DDMI ~ ADG))[[1]][[1]])
))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data) {
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (with(hw1Data, lm(ADG ~ DDMI))[[1]][[1]]),
b1_DDMIonADG = (with(hw1Data, lm(DDMI ~ ADG))[[1]][[1]])
))
}
hw1pt2(hw1Data)
# Execute the following code (you may need to modify the trait names to the right
# of the <- depending upon how you input your data) in R
no3.adg.ddmi <-  with(hw1Data, lm(ADG~DDMI))
no3.ddmi.adg <-  with(hw1Data, lm(DDMI~ADG))
summary(no3.adg.ddmi)
summary(no3.ddmi.adg)
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (with(hw1Data, lm(ADG ~ DDMI))[[1]][[1]]),
b1_DDMIonADG = (with(hw1Data, lm(DDMI ~ ADG))[[1]][[1]])
))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data) {
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (with(hw1Data, lm(ADG ~ DDMI))[[1]][[1]]),
b1_DDMIonADG = (with(hw1Data, lm(DDMI ~ ADG))[[1]][[1]])
))
}
hw1pt2(hw1Data)
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (with(data, lm(ADG ~ DDMI))[[1]][[1]]),
b1_DDMIonADG = (with(data, lm(DDMI ~ ADG))[[1]][[1]])
))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data) {
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (with(data, lm(ADG ~ DDMI))[[1]][[1]]),
b1_DDMIonADG = (with(data, lm(DDMI ~ ADG))[[1]][[1]])
))
}
hw1pt2(hw1Data)
# Execute the following code (you may need to modify the trait names to the right
# of the <- depending upon how you input your data) in R
no3.adg.ddmi <-  with(hw1Data, lm(ADG~DDMI))
no3.ddmi.adg <-  with(hw1Data, lm(DDMI~ADG))
summary(no3.adg.ddmi)
summary(no3.ddmi.adg)
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (with(data, lm(ADG ~ DDMI))[[1]][[2]]),
b1_DDMIonADG = (with(data, lm(DDMI ~ ADG))[[1]][[2]])
))
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data) {
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (with(data, lm(ADG ~ DDMI))[[1]][[2]]),
b1_DDMIonADG = (with(data, lm(DDMI ~ ADG))[[1]][[2]])
))
}
hw1pt2(hw1Data)
# Execute the following code (you may need to modify the trait names to the right
# of the <- depending upon how you input your data) in R
no3.adg.ddmi <-  with(hw1Data, lm(ADG~DDMI))
no3.ddmi.adg <-  with(hw1Data, lm(DDMI~ADG))
summary(no3.adg.ddmi)
summary(no3.ddmi.adg)
View(hw1Data)
View(hw1Data)
sireID <- hw1Data$sire[1]
whit(hw1Data,sireID %in% sire)
with(hw1Data,sireID %in% sire)
with(hw1Data, sire %in% sireID)
hw1Data[with(hw1Data, sire %in% sireID),]
selectedSire <- hw1Data[with(hw1Data, sire %in% sireID),]
mean(selectedSire[,3:4])
apply(selectedSire[,3:4],2,mean)
apply(selectedSire[,3:4],2,sd)
# Calculate means and SD for sires. If you wanted to increase ADG, which sire would
# you choose to be a parent in the next generation? If you wanted to decrease DDMI,
# which sire should you choose?
sireInformation <- function(sireID){
selectedSire <- hw1Data[with(hw1Data, sire %in% sireID),]
apply(selectedSire[,3:4],2,mean)
apply(selectedSire[,3:4],2,sd)
}
sapply(unique(hw1Data$sire),sireInformation)
apply(selectedSire[,3:4],2,mean)
apply(selectedSire[,3:4],2,sd)
c(apply(selectedSire[,3:4],2,mean),
apply(selectedSire[,3:4],2,sd))
# Calculate means and SD for sires. If you wanted to increase ADG, which sire would
# you choose to be a parent in the next generation? If you wanted to decrease DDMI,
# which sire should you choose?
sireInformation <- function(sireID){
selectedSire <- hw1Data[with(hw1Data, sire %in% sireID),]
c(apply(selectedSire[,3:4],2,mean),
apply(selectedSire[,3:4],2,sd))
}
sapply(unique(hw1Data$sire),sireInformation)
c(MEAN=apply(selectedSire[,3:4],2,mean),
SD=apply(selectedSire[,3:4],2,sd))
# Calculate means and SD for sires. If you wanted to increase ADG, which sire would
# you choose to be a parent in the next generation? If you wanted to decrease DDMI,
# which sire should you choose?
sireInformation <- function(sireID){
selectedSire <- hw1Data[with(hw1Data, sire %in% sireID),]
c(MEAN=apply(selectedSire[,3:4],2,mean),
SD=apply(selectedSire[,3:4],2,sd))
}
sapply(unique(hw1Data$sire),sireInformation)
# Calculate means and SD for sires. If you wanted to increase ADG, which sire would
# you choose to be a parent in the next generation? If you wanted to decrease DDMI,
# which sire should you choose?
sireInformation <- function(sireID){
selectedSire <- hw1Data[with(hw1Data, sire %in% sireID),]
c(MEAN=apply(selectedSire[,3:4],2,mean),
SD=apply(selectedSire[,3:4],2,sd))
}
sapply(unique(hw1Data$sire),sireInformation)
c(MEAN=apply(selectedSire[,3:4],2,mean),
SD=apply(selectedSire[,3:4],2,sd))[c(1,3,2,4),]
# Calculate means and SD for sires. If you wanted to increase ADG, which sire would
# you choose to be a parent in the next generation? If you wanted to decrease DDMI,
# which sire should you choose?
sireInformation <- function(sireID){
selectedSire <- hw1Data[with(hw1Data, sire %in% sireID),]
c(MEAN=apply(selectedSire[,3:4],2,mean),
SD=apply(selectedSire[,3:4],2,sd))[c(1,3,2,4),]
}
sapply(unique(hw1Data$sire),sireInformation)
c(MEAN=apply(selectedSire[,3:4],2,mean),
SD=apply(selectedSire[,3:4],2,sd))
# Calculate means and SD for sires. If you wanted to increase ADG, which sire would
# you choose to be a parent in the next generation? If you wanted to decrease DDMI,
# which sire should you choose?
sireInformation <- function(sireID){
selectedSire <- hw1Data[with(hw1Data, sire %in% sireID),]
c(MEAN=apply(selectedSire[,3:4],2,mean),
SD=apply(selectedSire[,3:4],2,sd))
}
sapply(unique(hw1Data$sire),sireInformation)
with(data, lm(ADG ~ DDMI))
with(data, lm(ADG ~ DDMI)
)
with(hw1Data, lm(ADG ~ DDMI)
)
coef(with(hw1Data, lm(ADG ~ DDMI)))
coef(with(hw1Data, lm(ADG ~ DDMI)))[1]
coef(with(hw1Data, lm(ADG ~ DDMI)))[2]
coef(with(hw1Data, lm(ADG ~ DDMI)))[[2]]
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (coef(with(hw1Data, lm(ADG ~ DDMI)))[[2]]),
b1_DDMIonADG = (coef(with(data, lm(DDMI ~ ADG))[[2]]))
))
hw1pt2 <- function(data) {
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (coef(with(hw1Data, lm(ADG ~ DDMI)))[[2]]),
b1_DDMIonADG = (coef(with(data, lm(DDMI ~ ADG))[[2]]))
))
}
hw1pt2(hw1Data)
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (coef(with(hw1Data, lm(ADG ~ DDMI)))[[2]]),
b1_DDMIonADG = (coef(with(data, lm(DDMI ~ ADG)))[[2]])
))
hw1pt2 <- function(data) {
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (coef(with(hw1Data, lm(ADG ~ DDMI)))[[2]]),
b1_DDMIonADG = (coef(with(data, lm(DDMI ~ ADG)))[[2]])
))
}
hw1pt2(hw1Data)
names(summary(no3.adg.ddmi))
# GENE613 - Homework 1
# Daniel Osorio - dcosorioh@tamu.edu
# Department of Veterinary Medicine and Biomedical Sciences
# Texas A&M University
# Read the data
hw1Data <- read.delim("../data/hw1data18.txt",stringsAsFactors = FALSE)
# 1. Give the minimum, the maximum, the mean, the variance, the standard deviation,
# and the coefficient of variation for the ADG and DDMI.
hw1pt1 <- function(data) {
return(c(
min = min(data),
max = max(data),
mean = mean(data),
var = var(data),
stdDev = sd(data),
CV = (sd(data) / mean(data))
))
}
apply(hw1Data[, 3:4], 2, hw1pt1)
# 2. Calculate the covariance of ADG and DDMI, the correlation of ADG and DDMI,
# the regression of ADG on DDMI, and the regression of DDMI on ADG, and explain
# the differences between these values
hw1pt2 <- function(data) {
return(c(
covariance = with(data, cov(ADG, DDMI)),
correlation = with(data, cor(ADG, DDMI)),
b1_ADGonDDMI = (with(data, lm(ADG ~ DDMI))[[1]][[2]]),
b1_DDMIonADG = (with(data, lm(DDMI ~ ADG))[[1]][[2]])
))
}
hw1pt2(hw1Data)
# Execute the following code (you may need to modify the trait names to the right
# of the <- depending upon how you input your data) in R
no3.adg.ddmi <-  with(hw1Data, lm(ADG~DDMI))
no3.ddmi.adg <-  with(hw1Data, lm(DDMI~ADG))
summary(no3.adg.ddmi)
summary(no3.ddmi.adg)
# Calculate means and SD for sires. If you wanted to increase ADG, which sire would
# you choose to be a parent in the next generation? If you wanted to decrease DDMI,
# which sire should you choose?
sireInformation <- function(sireID){
selectedSire <- hw1Data[with(hw1Data, sire %in% sireID),]
c(MEAN=apply(selectedSire[,3:4],2,mean),
SD=apply(selectedSire[,3:4],2,sd))
}
sapply(unique(hw1Data$sire),sireInformation)
hw1pt4 <- function(sireID){
selectedSire <- hw1Data[with(hw1Data, sire %in% sireID),]
c(MEAN=apply(selectedSire[,3:4],2,mean),
SD=apply(selectedSire[,3:4],2,sd))
}
sapply(unique(hw1Data$sire),hw1pt4)
out <- sapply(unique(hw1Data$sire),hw1pt4)
out <- sapply(unique(hw1Data$sire),hw1pt4)
out[1,]
max(out[1,])
min(out[3,])
rank(out[1,])
rank(out[3,])
rank(out[2,])
rank(out[4,])
